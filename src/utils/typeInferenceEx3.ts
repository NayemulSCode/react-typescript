// TypeScript automatically বুঝে যায়
const city = "ঢাকা"; // string type inferred
const population = 9000000; // number type inferred

// Function return type inference
function add(a: number, b: number) {
  return a + b; // return type 'number' inferred
}
